## What & why

<!-- One or two sentences. Link the issue if there is one. -->

## Sources

<!-- REQUIRED for any factual change (numbers, contacts, coordinates,
     translations): link the primary source. Write "n/a" for pure code. -->

## Checklist

- [ ] `pnpm typecheck` and `pnpm build` pass
- [ ] Every new user-visible string has both `en` and `ne` entries
- [ ] No new runtime dependencies (or discussed in an issue first)
- [ ] UI change: screenshot attached (including mobile width)
- [ ] No emergency numbers / donation channels added without a primary source
