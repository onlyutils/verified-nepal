node_modules/
dist/
.vite/
*.local
.DS_Store


# local env — dev values live in GitHub repo variables
.env

# local working dirs — not part of the project
docs/superpowers/
.impeccable/
.wrangler/

# e2e screenshots are kept out of git (see the review artifact); findings and reports stay in
docs/e2e/**/screenshots/
